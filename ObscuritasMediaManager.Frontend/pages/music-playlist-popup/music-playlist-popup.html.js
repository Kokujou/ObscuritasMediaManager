import { html } from '../../exports.js';
import { MusicPlaylistPopup } from './music-playlist-popup.js';

/**
 * @param {MusicPlaylistPopup} navItem
 */
export function renderMusicPlaylist(navItem) {
    return html` your html content `;
}
