import { html } from '../../exports.js';
import { WebcomponentTemplate } from './webcomponent-template.js';

/**
 * @param {WebcomponentTemplate} navItem
 */
export function renderWebcomponentTemplate(navItem) {
    return html` your html content `;
}
