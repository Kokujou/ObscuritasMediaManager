@inherits ElementBase

@if (vertical)
{
    <style>
        #star-container {
            flex-direction: column !important;
        }
    </style>
}
<div id="star-container" sword="@swords">
    @foreach (var x in Enumerable.Range(1, max))
    {
        <div class="star"
             hovered="@(singleSelect && hoveredRating > x)"
             selected="@(values.Contains(x))"
        @onclick="@(() => toggleRating(x))"
        @onpointerover="@(() => hoveredRating = x)"
        @onpointerout="@(() => hoveredRating = -1)">
            @(swords ? "⚔" : "★")
        </div>
    }
</div>